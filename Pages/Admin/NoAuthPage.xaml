<Page x:Class="baseline_system.Pages.Admin.NoAuthPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:baseline_system.Pages.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NoAuthPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="read bd" TextWrapping="Wrap" Style="{StaticResource TextBlockMainStyle}" FontSize="25"/>
            <Button Style="{DynamicResource menuButton}" Grid.Row="1" Content="Reset Setting" Click="Reset_Click" />
            <Button Style="{DynamicResource menuButton}" Grid.Row="2" Content="EXIT" Click="Exit_Click" />
        </Grid>
    </Grid>
</Page>
